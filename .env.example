# CutForge —— 可选的 Agent 默认配置(也可以完全在应用内"设置"里填写,
# 应用内配置保存在浏览器 localStorage,优先级更高)。
# 任一 OpenAI 兼容接口均可:OpenAI / GLM / DeepSeek / Kimi / Qwen / ...

VITE_LLM_BASE_URL=https://api.openai.com/v1
VITE_LLM_API_KEY=
VITE_LLM_MODEL=gpt-5-mini
